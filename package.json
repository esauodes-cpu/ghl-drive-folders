{
  "name": "ghl-drive-folders",
  "version": "1.0.0",
  "description": "Vercel serverless function to create Google Drive folder structures from GHL webhooks",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "googleapis": "^131.0.0"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  }
}
